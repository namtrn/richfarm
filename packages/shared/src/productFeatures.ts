/**
 * Product-scope switches for features that are intentionally staged behind
 * the current planning and planting experience.
 *
 * Keep the underlying data contracts intact while a feature is hidden so it
 * can be re-enabled without a migration when the product is ready.
 */
export const CARE_CONTENT_ENABLED = false;
